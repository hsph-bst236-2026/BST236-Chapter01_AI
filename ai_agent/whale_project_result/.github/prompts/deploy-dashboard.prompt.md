# Deploy Dashboard

Use the [dashboard-deployer](../agents/dashboard-deployer.md) agent to publish the latest dashboard at `docs/index.html` to GitHub Pages.

Commit the report to the `gh-pages` branch, push it, and print the live URL where the dashboard can be viewed.
